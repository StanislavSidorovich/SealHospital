# Тюленья больница — контекст проекта

Браузерная 3D-мини-игра, которую папа (Dad) делает для дочки Сабрины (9 лет). Она играет на телефоне или планшете. Весь интерфейс на русском, к игроку обращаемся на «ты» («Доктор Сабрина»).

- Репозиторий: https://github.com/StanislavSidorovich/SealHospital (публичный)
- Игра: https://stanislavsidorovich.github.io/SealHospital/ (GitHub Pages из `main`, корень)
- Та же игра как Claude Artifact (закрытая ссылка): https://claude.ai/artifact/AJnS2tyrX2A1QSZPkddMTA
- План работ: [ROADMAP.md](ROADMAP.md). Прежде чем начинать задачу, посмотри текущую фазу и отмечай сделанные пункты `[x]`.

## Технологии

- Three.js **r128**, лежит в `vendor/three.min.js` (UMD, глобальный `THREE`). CDN не используем, игра должна открываться без интернета.
- **Без сборщика и без ES-модулей**: только обычные `<script>` в нужном порядке (`vendor/three.min.js` → `js/i18n.js` → `js/data.js` → `letters.js` → `audio.js` → `world.js` → `seal.js` → `minigames.js` → `shop.js` → `shift.js` → `pet.js` → `walk.js` → `home.js` → `adventure.js` → `neighbors.js` → `mail.js` → `holidays.js` → `vendor/peerjs.min.js` → `net.js` → `coop.js` → `gull.js` → `rescue.js` → `game.js`). Файлы делят глобальную область: top-level `const/let/function` видны во всех следующих скриптах, порядок подключения важен. Стили в `css/style.css`. Игра обязана работать с `file://` (двойной щелчок), на GitHub Pages и как Claude Artifact.
- Шрифты Google Fonts: Pangolin (заголовки, рукописный каваи), Nunito (текст). Без сети подставляются системные, это нормально.
- Звуки синтезируются Web Audio, аудиофайлов нет. AudioContext создаётся только после нажатия.
- Сохранение в `localStorage`: один ключ `sh.save` = `{version, album, progress, muted, music, fish, shells, owned, decor, shifts, pet, mail, home, adv, hol, sea, nb, coop}` (`music` — фоновая музыка вкл/выкл, по умолчанию вкл; `pet` — свой тюленёнок или `null`, проверяется в `sanitizePet()`; `pet.seen` — когда навещали; `mail` = `{got:[{id, t}], d}` — полученные письма и день последнего «письма дня», `sanitizeMail()`; `home` = `{s:{слот: id мебели}, v}` — мебель в иглу малыша и заходили ли туда, `sanitizeHome()`, купленная мебель лежит в `owned`; `adv` = `{best:{уровень: звёзды}, cups, runs, day:{d, n}, tips, fish, sec, quest:{d, ids, done}}` — приключения: спасённые рыбки, найденные секреты, задания дня, `sanitizeAdv()`; `hol` — полученные праздничные подарки вида `'halloween-2026'`; `sea` = `{got, n, r}` — рыбки моря с рыбалки: пойманные виды, сколько всего уловов, на каком была последняя южная гостья, `sanitizeSea()`; `nb` = `{ping:{in, xp, q:{d, id, ok, got}}}` — сосед Пинг: живёт ли рядом, дружба 💙, просьба дня, `sanitizeNb()`; `coop` = `{wins, tries, net, day:{d, n}, gull:{d, n}, resc:{wins, day:{d, n}}}` — бой с Большой Тучей вдвоём, полёты чайкой в чужом забеге и спасённые потеряшки, `sanitizeCoop()`), объект `save` и `persist()` в `js/data.js`. У малыша ещё `walk` (прогулки за день), `finds` (находки) и `tricks` (звёзды трюков). Новое поле: значение по умолчанию в `sanitize()`; меняется смысл старых данных — поднять `SAVE_VERSION` и добавить функцию в `MIGRATIONS`. Старые ключи `sh.album/progress/muted` только читаются при миграции. Каждое чтение и запись оборачивать в try/catch (`store` уже так делает). Браузеру передаём просьбу «не стирай» через `keepSave()` (`navigator.storage.persist()`). **Код сохранения** (`makeCode()`/`readCode()` в `data.js`, окно `#settings` в `game.js`): `SEAL1.<сумма>.<base64 от save без album>`; любое новое поле сохранения автоматически попадает в код, а старые коды проходят через `upgrade()` (миграции + `sanitize`). Поменялся формат — подними `SAVE_VERSION`, а не `CODE_TAG`.
- PWA: `manifest.json`, `icons/` (192, 512, apple-touch). Service worker пока нет.

## Языки: русский и английский

- Переключатель **RU | EN** — на первом экране и в ⚙️ настройках. Язык живёт в `localStorage` под ключом `sh.lang` (настройка устройства, в код сохранения не входит), по умолчанию русский. Ссылка `…/SealHospital/?lang=en` включает английский и запоминает его. Переключение перезагружает страницу.
- `js/i18n.js` (грузится сразу после three.js): `LANG`, `L('по-русски', 'in English')`, `setLang()`, `nameL()` (имена пациентов в обе стороны: в альбоме лежат имена на том языке, на котором вылечили). **Любой новый текст игры пишем сразу двумя языками**: в JS через `L()`, в `index.html` через атрибуты `data-en` (текст), `data-en-aria`, `data-en-ph`, `data-en-alt` (русский остаётся в разметке как есть). Слова, зависящие от числа: `plural(n, one, few, many, enOne, enMany)`. Род малыша: `gg('он-форма', 'она-форма')`, по-английски — `gg('he', 'she')`.
- Данные с текстом (пациенты, лавка, находки, трюки, стадии, имена малышей) собираются при загрузке, поэтому `L()` стоит прямо в таблицах. Подписи фото малыша в альбоме хранятся ключами (`'intro'`, `'stage:2'`), `capL()` в `pet.js`.
- Письма папы: необязательное поле `en` в `LETTERS` (нет перевода — показывается `text`). Записка «Для любимой дочи — Dad ♡» по-английски: «For my dearest daughter — Dad ♡» (перевод мой, папа может поправить).
- Превью ссылки в мессенджерах: теги `og:*` в `index.html`, картинка `icons/og-image.png` (1200×630, снята из самой игры + плашка). WhatsApp и Telegram кешируют превью надолго; чтобы обновить, добавь к ссылке любой `?x=2` или почисти через https://developers.facebook.com/tools/debug/.

## Папина почта (Фаза 7)

- Письма — в `js/letters.js` (массив `LETTERS`: `text`, необязательные `date`, `gift`, `from`), папа дописывает строчку и пушит в `main`. Одно новое письмо в день по порядку списка; письмо с `date` приходит ровно в этот день (или до 7 дней позже). Письмо узнаётся по хешу текста (`letterId`), поэтому прочитанные письма не правим. Подарки: `{shells}`, `{hearts}`, `{wear:'dadhat'|'hearts'}` (вещи с `gift:true` в `SHOP` не продаются). Логика — `js/mail.js`.
- Тексты писем пишет папа. Черновики в `letters.js` можно предлагать, но не выдавать за папины без его просьбы.

## Визуальный стиль

- Каваи в духе Chiikawa, но **свои персонажи** (Chiikawa и Roblox не копировать): круглые тела, `MeshToonMaterial`, тёмный контур через back-face-оболочку (`addOutline`), глаза-точки с бликом, румянец, рот «ω».
- Палитра: чернила `#3B3A4A` (контуры, текст), розовый `#FF9BB8` / `#D9527E`, бумага `#FFFDF8`, мятный «готово» `#DDF5EA`, небо `#BDE5F4 → #EAF7FB`, вода `#6FC0DF`, льдина `#D6EAF5`.
- UI в стиле «наклеек»: рамка 2,5px цвета чернил, тень `0 4px 0` того же цвета, скругления 16–26px.
- Эмоции тюленя: `setMood(seal, 'sad' | 'ok' | 'happy')`.

## Правила игрового дизайна

- Проиграть нельзя. Ошибка = мягкая подсказка (тюлень мотает головой, всплывает тост). Никаких стрессовых таймеров.
- Ничего страшного, никаких покупок, рекламы и внешних запросов с данными. Исключение (решение папы, 25.09.2026): режим «Играем вместе» по сети (`js/net.js`, PeerJS) — передаются только ходы игры, не сохранение; только по коду комнаты, без текстового чата. Уточнение папы (24.09.2026): немного волнения можно (погоня, ворчливая Тучка, мультяшная акула), но опасность не догоняет насовсем, проиграть нельзя, в конце она смешная или добрая.
- Тексты короткие, крупные кнопки (не меньше 48px), одна главная вещь на экране.
- Записка от папы: «Для любимой дочи — Dad ♡». Текст не менять без просьбы папы.

## Как проверять

- Локальный сервер: `python -m http.server 8765` (если порт занят чужим сервером, бери другой, например 8766) из корня, затем открыть http://localhost:8765/.
- Встроенная панель браузера в Claude Desktop, когда она скрыта, почти останавливает `requestAnimationFrame`. Для проверок открывай `?test=1`: снимается ограничение на dt, а кадры подталкивает таймер (`frame()` каждые 60 мс). Мини-игры проверяй синтетическими касаниями: `mg.dispatchEvent(new PointerEvent("pointerdown", {bubbles:true, clientX, clientY, pointerId:1}))`.
- Размер экрана для проверки: мобильный 375×812.
- Кнопки удобно нажимать из JS: `document.getElementById('tool-scarf').click()`, `#btnStart`, `#hugBtn`, `#btnNext`, `#btnShift`, `#btnShop`.
- Одеяло в кроватке: `pointerdown` внизу `#mg`, затем `pointermove` вверх. Ракушки в событии: касание рядом с `toScreen(shell.position)`. Для лавки удобно положить ракушки: `save.shells = 200; persist()`.
- Малыш (Фаза 3): знакомство начинается, если `save.pet === null` и `save.shifts >= 1`. Уголок — кнопка `#btnPet` или `#btnIntroPet`, уход — `#pet-food`, `#pet-bath`, `#pet-sleep`, `#pet-fun`, `#pet-dress`. Проверить таяние потребностей: `save.pet.t = Date.now() - 10*3.6e6; persist()` и перезагрузить. Мяч: касание по `#mg`, когда виден `#mgStage .target`; пузыри — касание у `toScreen()` спрайта с `SOAP_TEX`; сон — медленные `pointermove` (до ~1500 px/с) рядом с малышом.
- Облачко-желание: все `save.pet.needs[k] = 1`, в уголке через ~4–6 с появится `petWish` (`pat|walk|tricks|ball`); касание у `toScreen(petSeal.bubble.getWorldPosition(new V3()))` на `#c` запускает занятие (`wishGo()`). Задать вручную: `petWish = 'walk'; petRefresh()`.
- Рост (Фаза 3, часть 2): стадии `STAGES` в `pet.js` по сердечкам (`pet.xp / HEART_XP`), отпразднованная стадия — `save.pet.stage`. Праздник: `save.pet.xp = 340; persist()` → в уголке через ~2 с начнётся `petGrow()`, окно `#grow`, кнопка `#btnGrowOk`. Ласка: `pointerdown` по малышу на `#c`, затем `pointermove` того же `pointerId` (≥500 px пути = +1 💗, не больше 6 раз в день, `save.pet.pat`).
- Прогулка и трюки (Фаза 3, часть 3, `js/walk.js`): `#pet-fun` открывает выбор `.picks [data-k="ball|walk|tricks"]`. На прогулке следующая льдинка и находки отмечены `#mgStage .target` — касание в его `left/top`; рыбка — группа с цветом `0xFFA552` в `scene.children`, пока видна; горка — `pointerdown` + много `pointermove` у кружка; в конце `#walkHome`. Новая находка — раз в день: `save.pet.walk = {d:'', n:0}; persist()`. Трюки: `.trick[data-id="paw|jump|roll|ball"]`; «ласта» — касание по `.target`, прыжок — `pointermove` вверх >70 px от `.swipe-up`, кувырок — круг радиусом ~80 px вокруг `.circle-hint`, мяч — касание, когда виден `.target`. Трюки старших стадий: временно `save.pet.stage = 3` в памяти (не забудь вернуть).
- Почта (Фаза 7, `js/mail.js`): письмо ждёт, если `mailWaiting` не `null`. Открыть: `#btnMail` или касание по ящику (`toScreen(MAILBOX_POS.clone().add(new V3(0,0.85,0)))` на `#c`), затем `#envelope`, `#mailGift`, `#btnMailOk`. Следующее письмо «завтра»: `save.mail.d = ''; mailRefresh()`. В уголке малыш держит письмо (`petSeal.letter`) — касание по малышу. Все письма заново: `save.mail = {got:[], d:''}; persist()`.
- Иглу (Фаза 4, `js/home.js`): в уголке кнопка `#btnHome` или касание по иглу → `homeEnter()`; внутри `#homeEditBtn` («Обустроить»), `#homeOutBtn` («Выйти»). Выбор мебели: `homePick('bed'|'rug'|'lamp'|'tank'|'window'|'shelf'|'pic')`, карточки `.home-pick .item[data-id]`, кнопки `#homeBuy`, `#homeCancel`. Касание по вещи на `#c` (`toScreen(homeItems.bed.getWorldPosition(new V3()))`, `pointerdown` + `pointerup`) — малыш играет (`homePlay(k)`); значки ➕/✏️ — `homeMarks[k]`; провести пальцем — поворот (`camFocus.yaw`). Чтобы малыш не бродил сам во время проверки: `homeIdleT = 999`. Комната стоит далеко (`HOME_POS` x=200), внутри приглушён свет (`HOME_LIGHT`) и нет тумана.
- Приключения (Фаза 9, `js/adventure.js`): `#pet-fun` → `.picks [data-k="run"]` → карта `.adv-map`, остров `.isle.on` (или `[data-lv=""]` — «Потом»). В забеге касание по `#mg` = прыжок, касание рядом с `toScreen(b.o.position)` пузыря из `R.bubbles` = спасти рыбку. Состояние — `R` (`z`, `y`, `air`, `shells`, `fish`, `state: ready|go|end`), дорожка стоит у `RUN_POS` (x=−200), камера — `runCam`. Бот для проверки: раз в 20 мс жать, если до `R.drifts[i].z` или `R.cracks[i].z0` осталось меньше 2,3 м, и тыкать в пузыри ближе 12 м. Итоги — `.run-end`, кнопки `[data-k="home"|"again"]`. Ракушки с дорожки идут в копилку только в первых `RUN_DAILY` = 2 забегах за день, дальше звёздочки: сброс — `save.adv.day = {d:'', n:0}`. Подсказки с замедлением — только пока `save.adv.runs === 0`. Пауза: `#btnRunHome` (в углу во время забега) → `.run-pause [data-k="go"|"home"]`; `R.paused`.
- Забег, часть 2: полосы — свайп по `#mg` (`pointerdown`, `pointermove` на 60 px вбок, `pointerup`), прыжок — `pointerdown` + `pointerup` без сдвига (прыжок на отпускании); на компьютере стрелки/пробел. `R.lane` (−1/0/1), `R.x`, `R.boost`, `R.ramp`. Уровень 2 `bay2` открывается, когда у `bay1` есть звезда; в нём `R.cloud`, `R.gap` (расстояние до Тучки), `R.snowballs` (летящие снежки: `ln` — полоса, `tz` — куда упадут); щекотка — касание рядом с `toScreen(R.cloud.position)`, когда `R.gap < 10.5`. Быстрый финиш: `R.z = 292` в состоянии `go`. Подсказки «один раз» лежат в `save.adv.tips` — очистить `[]`, чтобы увидеть снова. Добрая Тучка над уголком видна, если `save.adv.best.bay2 > 0`. Бот для полос: уходить с полосы, если там `R.drifts[i].ln === R.lane` ближе 4 м или падает снежок. В скрытой панели кадры бывают по 0,1–0,2 с — проверяй на видимой.
- Рыбки и секреты (Фаза 9): у каждого пузыря своя рыбка из `RUN_FISH` (`seal.js`, по 5 на уровень, `makeRunFish()`, `fishDef()`), спасённые — в `save.adv.fish` и в аквариуме иглу (`tankFish()`, в круглом 3, в большом все); касание по аквариуму (`homePlay('tank')`) открывает `.fish-panel`. Секрет — предмет `secret` в куске уровня (`keep:true`), в `R.pick` с `secret:true`; найденные — `save.adv.sec`.
- Трек дня: уровень собирает `dayLayout(id)` из `intro`, перемешанных `pool` и `finale` по дате (`advDayKey()`); другой день для проверки: `advDayKey = () => 'Sat Sep 26 2026'`. Задания дня — `QUESTS`, `advQuests()`, `save.adv.quest`; новые задания: `save.adv.quest = {d:'', ids:[], done:[]}`. В забеге счётчики `R.jumps`, `R.lanes`, `R.bonks`.
- «Скорая» (Фаза 6, `evNet()` в `shift.js`): событие смены, когда `save.shifts % 3 === 2`. Быстро: запустить смену, `mgClose(); scene.remove(S.seal.root); setBusy(false); evNet()`. Узелок развязывается кругом пальца: `pointerdown` у центра `.net-hint`, потом `pointermove` по кругу радиусом ~50 px (≥ 1,6π).
- Сосед Пинг (Фаза 10, `js/neighbors.js`): переезжает, если `pengMet()` (Пинг в альбоме) и есть малыш — в уголке через ~3,5 с `nbMoveIn()`; вернуть до переезда: `save.nb = sanitizeNb(null); persist()` и перезагрузка. Модель — `ping`, льдинка — `nbRoot` (`NB_POS`, стоит за табличкой, проверено на 375×812). Касание: `toScreen(ping.root.position.clone().add(new V3(0,0.5,0)))` на `#c` (`pointerdown` + `pointerup`). Мяч втроём: `.picks [data-k="ping"]` или `funPre = 'ping'; petDo('fun')`; дальше как мяч малыша — касание по `#mg`, когда подсказка «Нажми…» или виден `.target`. Просьба дня — `save.nb.ping.q`; выполнить вручную: `nbEvent(pingAsk(save.nb.ping.q.id).ev)`; новая «завтра»: `save.nb.ping.q.d = ''` (если подарок забран). Пока Пинг занят ужимкой — `nbIdle`, сценой — `nbBusy`.
- Скриншот в скрытой панели обрезан и может быть старым: кадр сцены (без DOM) можно выгрузить в файл — `renderer.render(scene, camera)`, `canvas.toDataURL('image/jpeg')` и `fetch` POST на крошечный локальный приёмник на Python в scratchpad, потом открыть jpg через Read.
- Играем вместе (Фаза 13, `js/net.js` + `js/coop.js`): `#btnIntroCoop` на первом экране или `.picks [data-k="coop"]` в ⚽ → `.co-pick [data-k="net"|"ping"|"solo"]`. Бой — `CO` (`me`, `pal`, `B` — Туча: `hp`, `ph` 0–2, `st: intro|go|change|wipe|win`, `H` — опасности), кадр — `coStep()`. Быстро: `CO.B.hp = 1` (фаза дальше), `coWin()`, `coBubble(CO.me)`, `CO.me.inv = 999` (не попадают), `CO.me.tx = 2` (бежать к x). Сеть проверяй двумя вкладками одного сервера: во второй `tabs_create` → тот же адрес; в первой `.net-lobby [data-k="host"]`, код — `net.code` (цифры = индексы `NET_EMO`), во второй `[data-k="join"]` и `.net-emo [data-e="…"]`. Фоновая вкладка медленная: таймеры пузырей идут не одновременно. Сеть есть только на https и localhost (`netAvail()`); `netClose()` перед перезагрузкой. Ретранслятор TURN у PeerJS умер (сентябрь 2026) — без него телефоны за разными роутерами / на мобильном интернете не соединяются (`timeout`). TURN берём у Metered: `NET_TURN = {app, key}` в начале `net.js`, `netIce()`; пустой ключ — только STUN. Причина ошибки видна мелко под текстом (`netWhy`, `no turn` — ключа нет). Встроенный браузер не резолвит имена ICE-серверов — TURN проверяй с телефона.
- Папа-чайка (Спринт 3, задача 2, `js/gull.js`): бегущий — `GL.role === 'run'` (карта → `.gull-call` → `gullCall()`), чайка — `GL.role === 'fly'` (`gullFly(pal)` из `coopFight`, если `pal.want === 'run'`). События бегущего — `gEv()` в `adventure.js`, команды чайки — `gullCmd({k:'drop'|'hl'|'sec'|'buoy'|'peck'|'peckd'|'tickle'})`, касание чайки — `gullTap(x, y)`. **Во встроенном браузере WebRTC сейчас не соединяется даже внутри одной страницы**: для связки двух вкладок подменяй транспорт — `BroadcastChannel`, свой `netLobby` (выставляет `net.conn = {open:true, send:m => bc.postMessage(m)}` и `net.host`), входящие — `net.on[m.t](m)`; вкладки должны «постучаться» друг к другу до `coHello`, иначе второй уйдёт в бой по таймауту. Проще проверять по одной стороне: у чайки — `net.conn = {open:true, send:m => out.push(m)}`, `gullFly({...})`, `net.on.rgo({id, day:advDayKey(), stars:false, left:5})` и таймер с `net.on.rs({z, x, y, v, st:'go', ...})`; у бегущего — вручную `GL = {role:'run', ...}` + `gullRunWire()` до старта забега. Фоновая вкладка слишком медленная для забега — бегущий должен быть на переднем плане. Вторая вкладка панели иногда закрывается сама.
- Чайка на острове (`gullIsle()` в `gull.js`): забег кончился — чайка не уходит, а порхает над уголком бегущего (`GL.home`, у папы `GL.isle`, сообщения `gisle`, `ip` — где малыш, `gip` — где чайка, `gc {k:'pat'}` — погладить). Плашка у Сабрины — `#gullChip` (💗 🎤 👋 «отпустить»), видна только в уголке. Новый забег (`rgo`) возвращает обоих на дорожку. `gullRunEnd(true)` — отпустить совсем (так делает «Вместе», перед новой игрой).
- «Спасаем потеряшку» (`js/rescue.js`, Спринт 3, задача 3): «Вместе» → вкладка `.co-games [data-g="rescue"]` → `[data-k="net"|"ping"|"solo"]` → `.rs-roles [data-r="jump"|"strong"]` (по сети роли выбирает хозяин, гость ждёт `qstart`). Состояние — `Q` (`me`, `pal`, `st`, `cage`, `asked`, `won`), мир — `RSW` (`gates`, `wall`, `plates`, `levers`, `see`, `block`, `lift`, `ices`), уровень — `RS_LV` (x вдоль, земля y=0, скала y=3). Кнопки: `.rs-l`, `.rs-r` (держать), `.rs-jump`, `.rs-act` (🔧 рычаг / ⬇️ нырнуть, `dataset.k`), `.rs-swap` (🔄 без напарника), `.rs-tip` 💡. Быстро: `Q.me.x = 52.2; Q.me.y = 3.2` (телепорт), `RSW.levers.forEach(l => l.on = true)`, `rsLever(i)`, `rsCage()`, `rsMom(i)` (правильная — `Q.moms.findIndex(m => m.ok)`), `rsBubble(Q.me)`. Прогон без кадров: `mgTicks.clear()` и свой цикл `now += dt; updateTweens(); rsStep(dt)` (async-сцены — через `await` между кусками). Сеть проверяй iframe-гостем на той же странице (вторая вкладка панели закрывается сама): оба окна — `BroadcastChannel`, `net.conn = {open:true, send:m => bc.postMessage(m)}`, входящие — `net.on[m.t](m)`, потом `coopNet('rescue')` сначала у хозяина.
- Праздники (`js/holidays.js`): `?holiday=halloween|newyear|valentine|bday` показывает праздник в любой день; подарок один раз в год (`save.hol`). День рождения Сабрины — `BDAY = [месяц, число]` (год не писать). Украшения в `holRoot`, расставлены под экран 375×812 — новые проверяй `toScreen()` на телефоне.
- Рыбалка (`fishCast()` в `minigames.js`): `#tool-fish` у голодного пациента или `#pet-food` у малыша (своя лунка `PET_HOLE`). Через ~1,5 с появляется `.reel`: если у неё класс `is-hold` — держать (`pointerdown` на `#mg`), пока `.knob` (`style.left` в %) внутри `.zone` (`left`/`width`), иначе тапать, пока есть `.reel-taps i:not(.pop)`. Виды — `SEA_FISH` в `seal.js`, выбор — `pickCatch()`; южную гостью сразу: `save.sea = {got:[], n:5, r:-10}`. Карточка нового вида — `.sea-card`, кнопка `#seaOk`. Лупа: радиус `HOT_R`, время `HOT_T`; приметы `s.rumble` (животик), `s.frost` (иней на ластах).
- Выход из больницы посреди лупы или лечения: `#btnPet` вызывает `mgPark()` (мини-игра откладывается в `mgParked`), обратно — `mgUnpark()` в `goPet(false)`. Отложить можно только в стадиях `diagnose`/`treat` и пока `mgEnding` не выставлен (игра уже решена). Пациент приходит (стадия `arriving`) — кнопка по-прежнему говорит «Сначала закончи».
- Язык: `?lang=en` в адресе (или кнопки `.lang button[data-lang]`); в тестах после смены языка перезагрузи страницу. Новые строки проверяй в обоих языках; в английском длинные подписи под кнопками (например «Thermo») не должны вылезать за 66px.
- Настройки/код: `#btnSettings` (или `#btnIntroSet` на первом экране), `#codeBox`, `#btnCodeCopy`, `#btnCodePaste`, `#btnCodeYes`. Автоматизированная панель не даёт `navigator.clipboard`, поэтому «Скопировать» там покажет запасное сообщение; проверяй так: `codeBox.value = makeCode()` → `#btnCodePaste`. «Я скучал!»: `save.pet.seen = Date.now() - 50*3.6e6; persist()` и перезагрузка (правка `localStorage` вручную не сработает: при закрытии вкладки `pagehide` дописывает `save` из памяти).
- Пингвин-самозванец (Фаза 6, конец `js/shift.js`): приходит вторым пациентом, если `pengDue()` (`shift.n === 1`, `save.shifts >= 1`, Пинга ещё нет в альбоме или шанс 25%). Быстро вызвать: запустить смену, `mgClose(); scene.remove(S.seal.root); setBusy(false); save.shifts = 1; shift.n = 1; spawnPatient()`. Улики под лупой — `hotNear(S.seal, 'beak'|'paws'|'quack', 0, 0).p` (лупа смотрит на 70 px выше пальца). Кнопки `#pengYes`, `#pengNo`; дальше `#tool-scarf`, `#scarfDone`, `hug()`.
- Звук (Фаза 12): все звуки в `js/audio.js` идут через `BUS` (компрессор), ноты — `pn(n)` (пентатоника, 0 = C5), инструменты `marimba`/`bell`/`squeak`/`drop`. Новый звук — добавить в `sfx` и в список `LIST` пульта `sounds.html` (http://localhost:8766/sounds.html: «было / стало», 👍/👎). Громкость сравнивать офлайн: `OfflineAudioContext` + подмена `AC`/`BUS`.
- Музыка (Фаза 12, часть 2, конец `js/audio.js`): мелодии `TUNES`, место выбирает `musicMood()` (`hosp|pet|home|run|map|night`), текущее — `musMood`, громкость — `MUS.gain`. В скрытой панели `document.hidden === true`, и музыка молчит — для проверки `Object.defineProperty(document, 'hidden', {get:() => false, configurable:true})`. Мелодию вручную: `musForce = 'run'` (на пульте `sounds.html` — кнопки «🎵 Музыка»). Переключатель — `#musicSeg [data-m="1"|"0"]` в ⚙️. Уровень замерять `AnalyserNode` на `MUS`: пик музыки ≈0,035–0,04 при пике касания ≈0,09.
- Встроенный браузер кеширует js: после правок делай `await fetch('js/имя.js', {cache:'reload'})` и перезагрузку, иначе смешаются старые и новые файлы. Лог консоли переживает перезагрузки — старые ошибки не значат новых.
- Когда панель браузера скрыта, скриншоты обрезаются. Проверяй через JS или открой панель. Скриншот иногда показывает прошлый кадр: подожди ~0,5 с и сними ещё раз, а цвет точнее проверять `gl.readPixels` сразу после `renderer.render(scene, camera)`.
- Правка сохранения для проверки: меняй `save` в памяти и `persist()`, потом перезагрузка. `localStorage.setItem` перед переходом не работает: `pagehide` старой страницы перезапишет его.

## Публикация

- GitHub Pages: достаточно пуша в `main`.
- Обновление Artifact: Artifact publish с `url` из списка выше, `file_path` = `index.html`, и `files` со всеми остальными файлами: `vendor/three.min.js`, `vendor/peerjs.min.js`, `css/style.css`, `js/*.js` (с `js/gull.js`), `manifest.json`, `icons/*.png` (путь публикации = путь файла). Сначала сделай artifact read, потом republish.
- Git: коммиты от `StanislavSidorovich` с noreply-адресом (он уже в локальном git config). Личный email в коммиты не ставить.

## Модели

Новую механику или фазу начинать на Opus, контент, баги и мелкие правки делать на Sonnet (подробнее в ROADMAP.md). Папа любит, когда предлагают улучшения: в конце задачи коротко предлагай 1–3 идеи.
